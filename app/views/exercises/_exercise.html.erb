<tr class="border-t border-">
  <td class="px-5 py-5 bg-white rounded-bl-xl">
    <%= exercise.name %>
  </td>
  <td class="px-5 py-5 bg-white">
    <%= exercise.video_url %>
  </td>
  <td class="px-5 py-5 bg-white">
    <%= exercise.objective %>
  </td>
  <td class="px-5 py-5 bg-white rounded-br-xl">
    <%= link_to t('buttons.edit'), edit_exercise_path(exercise), class: "bg-accent text-white font-bold py-2 px-4 rounded-xl border border-accent hover:bg-background hover:text-accent" %>
    <!-- <%= link_to t('buttons.delete'), exercise, method: :delete, data: { confirm: 'Are you sure you want to delete this exercise?' }, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %> -->
  </td>
</tr>
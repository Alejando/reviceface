<tr class="border-t border-">
  <td class="p-5 bg-white rounded-bl-xl">
    <%= exercise.name %>
  </td>
  <td class="p-5 bg-white">
    <%= exercise.video_url %>
  </td>
  <td class="p-5 bg-white">
    <%= exercise.objective %>
  </td>
  <td class="bg-white rounded-br-xl">
    <div class="flex items-center justify-start">
      <%= render "shared/edit_icon", path: edit_exercise_path(exercise), turbo_stream: false %>
    </div>
    <!-- <%= link_to t('buttons.delete'), exercise, method: :delete, data: { confirm: 'Are you sure you want to delete this exercise?' }, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %> -->
  </td>
</tr>
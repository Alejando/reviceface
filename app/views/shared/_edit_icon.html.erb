<%= link_to path, data: { turbo_stream: turbo_stream }, class: "group inline-flex items-center justify-center p-2 rounded-lg text-primary hover:bg-primary hover:text-background transition-colors duration-200 mx-1" do %>
  <%= icon "pencil", class: "h-5 w-5 group-hover:text-background transition-colors duration-200", data: { color: "#5B8DAE" } %>
<% end %>
